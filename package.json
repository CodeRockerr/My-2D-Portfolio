{
  "name": "adit-shah-portfolio",
  "private": true,
  "scripts": {
    "dev": "npm --prefix vite-project run dev",
    "build": "npm --prefix vite-project run build",
    "preview": "npm --prefix vite-project run preview",
    "test": "npm --prefix vite-project test",
    "test:browser": "npm --prefix vite-project run test:browser"
  }
}
